pipeline {
  agent any
  stages {
    stage('Unit Test') { 
      steps {
        
      }
    }
    stage('Deploy Standalone') { 
      steps {
        
      }
    }
    stage('Deploy ARM') { 
      environment {
        ANYPOINT_CREDENTIALS = credentials('anypoint.credentials') 
      }
      steps {
        
      }
    }
    stage('Deploy CloudHub') { 
      environment {
        ANYPOINT_CREDENTIALS = credentials('anypoint.credentials')
      }
      steps {
       
      }
    }
  }
}